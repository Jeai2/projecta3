# 天音(천음) 깊이 기획 — 못생겨도 속이 꽉 찬, 깊은 점법

작성일: 2026-06-18
상태: 기획(아이디어 확장 단계). 출시/결제/배포는 별도 트랙. 해석 콘텐츠(신패/진패 문장)는 파운더가 직접 작성하며, 본 문서는 **그 콘텐츠를 담을 구조·규칙·기능·경험**만 다룬다.

---

## 0. 이번 결정 (2026-06-18)

- **(A)** 본 기획을 정본 문서로 저장.
- **스프레드 전부 완성**: 일기·양의는 완료 → **통관·천지인·원형이정·순환·낙서구궁** 구축.
- **기둥 A(패↔패 간지 관계 엔진)는 3장 이상 스프레드부터 적용.**
  - 일기(1장): 제외 (관계 대상 없음). 단 시지(時支)×신패 관계는 별도 규칙으로 채움.
  - 양의(2장): **제외.** 양의는 카드 간 관계가 아니라, 각 패 천간을 그 흐름의 "나"로 두고 점사시간 지지와 비교하는 **좌/우 미래 비교**를 유지.
  - 통관(3)·천지인(3)·원형이정(5)·순환(5)·낙서구궁(9): **관계 엔진 적용.**

### 완료 (2026-06-18)
- **관계 엔진은 새로 만들지 않고 앱 공용 canonical 데이터를 재사용함** → `server/src/data/relationship.data.ts`(천간합/합화/충, 육합/삼합/방합/암합 + 합화 오행, 육충/육형/육파/육해). `cheoneum-relations.ts`는 이 데이터를 카드(라벨+간지)에 적용하는 얇은 어댑터일 뿐.
  - ⚠️ 이 프로젝트의 `천간충`은 인접 극(예: 甲戊) 방식으로 정의되어 있음(교과서적 甲庚충과 다름). 앱 전체 일관성을 위해 그대로 따름. 바꾸려면 `relationship.data.ts` 한 곳만 고치면 전부 반영됨.
- **3장↑ 스프레드에 `[카드 사이 관계]` 블록 주입** (`cheoneum-divination.service.ts`). 일기·양의 제외.
- **5개 스프레드 포지션-역할 판독 reducer 완료** (`cheoneum-spread-reading.ts`): 통관(열쇠), 천지인(천/인/지 + 음 반전), 원형이정(元亨利貞 단계 연쇄), 순환(중앙↔사방), 낙서구궁(중앙↔8방). `[스프레드 구조 판독]` 블록으로 주입.

---

## 1. 다 자랐을 때의 모습 (비전)

든든해진다는 건 카드가 늘거나 화면이 화려해지는 게 아니다. **한 자리에 앉아 점 보는 동안, 패와 패가 간지로 엮이고, 그 한 장이 다음 펼침의 축으로 이월되며, 화선·화영이 "지난번 그 패가 또 나왔네요"를 먼저 짚어주는** 상태다. 콘텐츠 한 줄 안 늘려도 *같은 카드가 어느 사다리·어느 자리·어느 관계 속에 놓이느냐*에 따라 다르게 울리고, 파고들수록 한 겹씩 더 열린다.

핵심: **"구조가 깊어서 콘텐츠가 저절로 깊어 보이는 시스템."**

---

## 2. 깊이의 네 기둥

### 기둥 A — 점법 문법: 패와 패가 간지로 엮인다 (나머지의 토대)

지금은 모든 카드를 점사일/점사시간하고만 비교하는 *평면* 구조. 3층 문법으로 세운다.

- **관계 엔진(關係)**: `server/src/data/saju.data.ts`에 없는 관계 테이블(지지 육합/삼합/충/형/파/해, 천간 합/충, 오행 생극)을 깔고, `buildCheoneumRelations(cards) → RelationEdge[]`로 카드끼리 엮는다. 신패는 이미 `SINPAE_GANJI`로 간지가 있어 신패↔진패도 같은 함수로 처리.
  - *예: 통관에서 주체A=庚申·주체B=甲寅·통관패=巳 → 寅巳申 삼형 → "열쇠가 갈등을 풀기보다 먼저 휘젓는 자리".*
- **포지션 문법(axis/role)**: `CheoneumPositionRule`에 `axis·role·isAnchor·pairsWith` 추가. 양/음 반전을 "좌우 위치"가 아니라 **"기준점(나/일간)이 어느 자리로 가느냐"** 의 의미 반전으로 통일.
  - *예: 천지인 음에서 배치는 그대로, `role`만 top=earth/bottom=heaven로 반전.*
- **상위 스프레드 판독기(reducer)**: 스프레드마다 `(PlacedCard[], RelationEdge[]) → 구조 판독`을 만들어 산문 지침 대신 구조화된 결론을 프롬프트에 준다. 7개가 "비슷한 보고서"가 아니라 진짜 다른 점법으로 갈린다.
- **신패-진패 결합**: 한 스프레드 안 신패(큰 흐름)·진패(구체 사건)의 간지 관계 + 오행 생극으로 결합 양태 분류(생함=키운다 / 극함=누른다 / 지지 충=주제와 현실이 따로 논다).

### 기둥 B — 상담 의식: 챗봇이 아니라 한 호흡씩 깊어지는 한 자리

"질문→카드→해석" 1턴 거래를 상태머신으로.

- **의식의 결(ritualPhase)**: 세션에 단계 필드. 가늠 중엔 카드 안 뽑고, 마무리 단계엔 새 카드 금지.
- **점진적 개방**: 십성·주의점·조언을 한 번에 안 쏟고 겹으로 저장 → "왜?/그래서?/더"에 한 겹씩 개방. (이어가기 감지는 이미 배선됨)
- **펼침 합의 + 닫는 의식**: 상위 스프레드는 "엔진 제안→사용자 yes→펼침" 2단 게이트. 지금 전혀 없는 **마무리 봉인** 추가. (체감 큼·비용 작음)
- **가늠 리액션을 엔진에 묶기**: "흠..."을 십성 조합의 거칠기(severity)에 묶어 무거운 카드엔 무거운 가늠.

### 기둥 C — 한 존재와의 관계: 공명이 온도계가 된다

- **공명→관계 4단계**(낯섦→익숙→신뢰→속얘기) 매핑해 페르소나 프롬프트에 *행동지시*로 주입. (지금 resonance는 숫자로만 들어가고 "수치 말하지 마라"만 있음 — 행동 변화 0)
- **한 존재 장치**: 화선↔화영 교대 시 "직전 얼굴이 남긴 메모 1줄"을 새 얼굴에 주입 → 둘이 한 존재임이 드러남.
- **게이밍 방지**: 복붙 반복=0/감점, 며칠 뒤 같은 고민 이어옴=보너스, defense 중엔 적립 정지.

### 기둥 D — 시간이 쌓이는 점사 기록(占史) ("속이 꽉 찬"의 핵심)

- **ReadingRecord 영속화**: 뽑은 패/배치/간지/도메인을 휘발시키지 말고 조회 가능한 레코드로. *이 한 테이블이 D 전체의 토대.*
- **반복 주제 감지 + '그때 그 카드' 콜백**: "24일간 연애 4번" / "월영이 또 나왔다"를 술사가 먼저 짚음. 신패 12(잦은 재등장) vs 진패 60(드문 재회)의 확률차를 서사 강도로.
- **세션 밖 누적 동조도(affinity)**: 30분 리셋 세션 resonance와 별개로, 기록 수·기간 기반 누적 친밀도.

> **세계관/lore**는 별도 기둥이 아니라 A의 메타데이터·C의 보상으로 흡수: 신패12="천극→태허로 닫히는 천도", 진패60="오행 5군단×12지", tool_question은 공명 높을수록 깊은 설정 개방.

---

## 3. 레버리지 순 (채우면 깊이가 폭발하는 곳)

1. **관계 엔진 + 관계 테이블** — 압도적 1순위. 지금 카드 N장이어도 관계 N(N-1)/2개를 다 버림. 하나 얹으면 상위 5개 스프레드가 한꺼번에 깊어짐.
2. **일기 시지(時支)×신패 규칙** — `hourJi`를 계산해놓고 일기에서 버림(`divination.service.ts`). 룰 문서가 유일하게 "별도 규칙 필요"라 못 박은 미완 항목.
3. **상위 스프레드 판독 reducer** — 1·2가 데이터 깔면 그 위에서 7개가 진짜 갈림.
4. **공명→프롬프트 행동지시** — 굴러가는 숫자가 행동을 0으로 바꾸는 게 제일 아까움.

---

## 4. 스프레드 완성 작업 분해 (오늘 착수 합의)

순서: **관계 엔진(토대) → 3+ 스프레드별 판독 → 양/음 반전 의미론 → 프롬프트 주입.**

1. **관계 엔진(기둥 A 토대)** — 지지 합/충/형/파/해 + 천간 합/충 + 오행 생극 테이블, `buildCheoneumRelations(cards)`. (`saju.data.ts` 위 + `cheoneum-divination.service.ts`에 `[카드 사이 관계]` 블록 주입)
2. **통관(3)** — 주체A·주체B·통관패(신패) 사이의 관계로 "열쇠가 푸는가/휘젓는가" 판독.
3. **천지인(3)** — 천/인/지 포지션 role + 양/음 role 반전 + 세 자리 간 관계.
4. **원형이정(5)** — 元亨利貞 4진패 인접 관계 연쇄 + 신패 앵커. 음은 포지션 의미 반전.
5. **순환(5)** — 중앙↔사방 관계, 양/음 신패·진패 위치 반전.
6. **낙서구궁(9)** — 중앙 신패 + 8방 진패, 시계/반시계 배치, 구궁 구조 판독.

각 스프레드는 **사실 태그**(어느 관계로 엮였나, 어느 자리에서 왕/쇠인가)만 프롬프트에 공급하고, 완성 문장은 파운더의 콘텐츠 + AI 변주가 채운다.

---

## 부록 — 근거 파일

- `server/src/cheoneum/cheoneum-divination.service.ts` (SINPAE_GANJI, buildIlgiInterpretationGuide, getDivinationHourGanji/hourJi, buildCheoneumDivinationInsight)
- `server/src/cheoneum/cheoneum.spreads.ts` (CheoneumPositionRule, polarity 분기 jinpae/sinpae 팩토리)
- `server/src/cheoneum/cheoneum-consultation.service.ts` (스프레드별 출력 지침, getResonanceDelta, resonance 프롬프트 주입부)
- `server/src/data/saju.data.ts` (GAN/JI/SIPSIN_TABLE/SIBIWUNSEONG_TABLE/GAN_OHENG/JI_OHENG — 합/충 관계 테이블 없음)
- `server/src/services/session.service.ts` (CheoneumSessionState)
- `CHEONEUM_SPREAD_RULES.md` (7개 스프레드 배치·양음 반전 규칙)

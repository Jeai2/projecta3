# 현재 작업 메모

작성일: 2026-06-02

## 현재 작업 주제

앱 첫 진입 Flow를 다듬는 중이다.

현재 초점은 `기존 스플래시 -> 문턱 이미지 -> 채팅 화면` 흐름을 자연스럽게 만드는 것이다.

## 현재 앱 진입 구조

```text
앱 실행
-> 네이티브 스플래시
-> 앱 내부 스플래시
-> 문턱 이미지 화면
-> [들어간다] 버튼
-> 채팅 화면
```

## 적용된 방향

- 로고 스플래시는 영상화하지 않고 기존 정적/코드 애니메이션 스플래시를 유지한다.
- 스플래시 이후에 별도 문턱 이미지를 보여준다.
- 문턱 이미지는 낮/밤 aspect에 따라 분기한다.
- 사용자가 `[들어간다]`를 누르면 추가 과정 없이 바로 채팅으로 넘어간다.
- 화면 전환 시 반짝임을 줄이기 위해 앱 루트에 aspect 기반 배경색을 깔았다.

## 낮/밤 분기

현재 기준:

```text
06:00 <= 현재 시각 < 18:00 -> 화선
그 외 시간 -> 화영
```

강제 테스트 값은 꺼져 있다.

```ts
const TEST_ASPECT_OVERRIDE: Aspect | null = null;
```

## 현재 사용 중인 문턱 이미지

```text
화선 / 낮:
mookseoli/assets/entrance-hwaseon.png

화영 / 밤:
mookseoli/assets/entrance-hwayeong.png
```

원본 의미:

```text
opam.png -> 낮 이미지
oppm.png -> 밤 이미지
```

## 현재 코드상 주요 위치

- `mookseoli/App.tsx`
  - `ASPECT_COPY`: 화선/화영 이미지, 배경색, 버튼 스타일 설정
  - `SplashScreen`: 기존 내부 스플래시
  - `EntranceScreen`: 문턱 이미지와 `[들어간다]` 버튼
  - `App`: `splash | entrance | chat` 단계 전환

## 현재 로컬 API 주소

```ts
const API_URL = 'http://192.168.219.102:3001';
```

네트워크가 바뀌면 이 IP는 다시 수정해야 한다.

## 바로 확인해야 할 것

1. `npx expo start -c`로 실행해서 새 이미지 캐시를 확실히 반영한다.
2. `스플래시 -> 문턱 이미지` 전환에서 반짝임이 줄었는지 본다.
3. `[들어간다]` 버튼 위치가 하단 시스템 영역과 겹치지 않는지 본다.
4. 낮/밤 각각 이미지가 의도한 aspect에 맞게 나오는지 본다.
5. 버튼 클릭 후 채팅 화면으로 자연스럽게 넘어가는지 본다.

## 아직 남은 판단

- `[들어간다]` 문구를 유지할지, `말을 건넨다` 같은 더 상담형 문구로 바꿀지 결정해야 한다.
- 화선/화영이 서버 프롬프트에 실제로 반영되는 작업은 아직 별도 단계다.
- 현재 프론트는 `/api/greeting`, `/api/chat`에 `aspect`를 보내지만, 서버 말투 분리는 아직 후속 작업이다.

